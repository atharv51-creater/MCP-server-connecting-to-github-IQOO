# Copy to .env and fill in for your demo repo
PORT=4000
GITHUB_WEBHOOK_SECRET=dev-secret-change-me
# The one directory the server is allowed to run git commands inside.
# Set this to the PARENT folder of your actual demo repo, e.g. /home/you/projects
ALLOWED_REPO_ROOT=.
